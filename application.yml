global:
  message: Welcome to golbal Microservices Environment!!
logging:
  level: 
    root:INFO
management:
  endpoints:
    web:
     exposure:
        include: health, info, refresh
     endpoint:
       health:
         show-details: always
